dubbo:
  consumer:
    timeout: 3000
    check: false
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://${wisps.nacos.server.url}
    parameters:
      namespace: ${wisps.dubbo.nacos.namespace}
      group: ${wisps.dubbo.nacos.group}
  application:
    name: ${spring.application.name}
    qos-enable: true
    qos-accept-foreign-ip: false